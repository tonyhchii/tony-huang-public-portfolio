{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": ".", // Set the base directory for your imports
    "paths": {
      "@/*": ["src/*"] // Alias '@' to point to 'src' directory
    }
  }
}
